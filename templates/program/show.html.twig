{% extends 'base.html.twig' %}

{% block title %}Série #{{ program.id }}{% endblock %}

{% block body %}
    <div class="media">
        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>
            <img src="{{ vich_uploader_asset(program) }}" alt="{{ program.title }}">
            <p>{{ program.synopsis }}</p>
            <p>Catégorie : {{ program.category.name }}</p>.
            <h2>Saisons disponibles :</h2>
            <ul>
                {% for season in program.seasons %}
                    <li><a href="{{ path('program_season_show', { 'program_slug': program.slug, 'season_id': season.id }) }}">{{ season.number }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <strong>Acteurs :</strong>
    <ul>
        {% for actor in program.actors %}
            <li><a href="{{ path('actor_show', {id: actor.id}) }}">{{ actor.name }}</a></li>
        {% else %}
            <li>Aucun acteur pour cette série</li>
        {% endfor %}
    </ul>
    <p>Durée de la série : {{ programDuration }}</p>
    <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a>
{% endblock %}